<% provide(:title, 'Create company') %>

  <h1>Create company</h1>
  <div class="row">
    <div class="span6 offset3">
      <%= form_for([current_user, @company]) do |f| %>
        <% # debugger %>
        <%#= hidden_field_tag(:user_id, current_user.id) %>
      
        <%= render 'shared/error_messages', :mesg => @company %>
      
        <%= f.label :name  %>
        <%= f.text_field :name  %>

     
        <br/>
        <%= f.submit "Create company", class: "btn btn-large btn-primary"  %>

      <% end %>
    </div>
  </div>